'use client'; import {useState} from 'react'; import {Check, Droplets, Moon, Footprints, Salad} from 'lucide-react';
const habits=[['น้ำ 2.5 ลิตร',Droplets],['เดิน 6,000 ก้าว',Footprints],['ผักอย่างน้อย 2 มื้อ',Salad],['เตรียมตัวนอนให้ตรงเวลา',Moon]];
export default function Journey(){const [done,setDone]=useState<boolean[]>([false,false,false,false]); const toggle=(i:number)=>setDone(x=>x.map((v,j)=>j===i?!v:v)); const pct=Math.round(done.filter(Boolean).length/4*100); return <main className="mx-auto max-w-4xl px-5 py-10"><div className="rounded-[2rem] bg-white p-7 shadow-sm"><div className="text-sm font-semibold text-emerald-700">Your Health Journey</div><h1 className="mt-2 text-4xl font-bold">Day 1 — เริ่มจากเรื่องเล็ก ๆ</h1><p className="mt-3 leading-7 text-slate-600">วันนี้เลือกทำสิ่งที่ดีให้ตัวเอง 4 อย่าง ไม่จำเป็นต้องทำได้ครบเพื่อถือว่าเก่ง</p><div className="mt-7 h-3 rounded-full bg-emerald-100"><div className="h-3 rounded-full bg-emerald-700 transition-all" style={{width:`${pct}%`}}/></div><div className="mt-2 text-right text-sm font-semibold text-emerald-700">{pct}% completed</div></div><div className="mt-5 space-y-3">{habits.map(([label,Icon]:any,i)=><button onClick={()=>toggle(i)} key={label} className={`flex w-full items-center gap-4 rounded-3xl border p-5 text-left transition ${done[i]?'border-emerald-200 bg-emerald-50':'border-slate-200 bg-white hover:border-emerald-200'}`}><span className={`flex h-12 w-12 items-center justify-center rounded-2xl ${done[i]?'bg-emerald-700 text-white':'bg-emerald-50 text-emerald-700'}`}>{done[i]?<Check/>:<Icon/>}</span><span className={`flex-1 font-bold ${done[i]?'text-emerald-800 line-through':''}`}>{label}</span></button>)}</div></main>}
