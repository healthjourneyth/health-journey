'use client'; import {useState} from 'react'; import {Send, Sparkles} from 'lucide-react';
export default function AICoach(){const [msg,setMsg]=useState(''); const [chat,setChat]=useState<any[]>([]); const send=(text=msg)=>{if(!text.trim())return; setChat(c=>[...c,{from:'me',text},{from:'ai',text:'ขอบคุณที่เล่าให้ฟังนะคะ 🌿 ในเวอร์ชันนี้ AI Coach ยังเป็นต้นแบบ แต่แนวทางของเราจะช่วยคุณแยก “สิ่งที่ทำได้วันนี้” ออกจากข้อมูลสุขภาพที่ควรปรึกษาผู้เชี่ยวชาญค่ะ'}]);setMsg('')}; return <main className="mx-auto max-w-3xl px-5 py-10"><div className="rounded-[2rem] bg-white shadow-sm"><div className="border-b p-6"><div className="flex items-center gap-3"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-emerald-100 text-emerald-700"><Sparkles/></span><div><h1 className="text-2xl font-bold">AI Coach</h1><p className="text-sm text-slate-500">ผู้ช่วยคิดเรื่องเล็ก ๆ ในชีวิตประจำวัน</p></div></div></div><div className="min-h-[420px] space-y-4 p-6">{chat.length===0&&<div><p className="font-semibold">ลองถามได้เลย</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{['วันนี้ควรเริ่มปรับอะไรดี?','หิวตอนดึกทำอย่างไร?','อยากนอนให้ดีขึ้นเริ่มตรงไหน?','ช่วยวางเป้าหมาย 7 วันให้หน่อย'].map(q=><button onClick={()=>send(q)} key={q} className="rounded-2xl border border-emerald-100 p-4 text-left hover:bg-emerald-50">{q}</button>)}</div></div>}{chat.map((m,i)=><div key={i} className={`flex ${m.from==='me'?'justify-end':''}`}><div className={`max-w-[85%] rounded-2xl px-4 py-3 ${m.from==='me'?'bg-emerald-700 text-white':'bg-slate-100 text-slate-700'}`}>{m.text}</div></div>)}</div><div className="flex gap-2 border-t p-4"><input value={msg} onChange={e=>setMsg(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="พิมพ์คำถามของคุณ..." className="flex-1 rounded-2xl border border-slate-200 px-4 py-3 outline-none focus:border-emerald-500"/><button onClick={()=>send()} className="flex h-12 w-12 items-center justify-center rounded-2xl bg-emerald-700 text-white"><Send size={18}/></button></div></div></main>}
