'use client';
import Link from 'next/link';
import Image from 'next/image';
import { usePathname } from 'next/navigation';
import { LayoutDashboard, ClipboardCheck, MessageCircle, Route, Menu, X } from 'lucide-react';
import { useState } from 'react';
const nav=[['/assessment','ประเมินสุขภาพ',ClipboardCheck],['/dashboard','แดชบอร์ด',LayoutDashboard],['/journey','เส้นทางของฉัน',Route],['/ai-coach','AI Coach',MessageCircle]] as const;
export function AppShell({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false); const path=usePathname(); return <><header className="sticky top-0 z-50 border-b border-emerald-100/80 bg-white/95 backdrop-blur"><div className="mx-auto flex h-18 max-w-6xl items-center justify-between px-5"><Link href="/" className="flex items-center gap-3"><Image src="/health-journey-logo.svg" alt="Health Journey" width={54} height={54} className="h-11 w-11 rounded-full"/><div><div className="font-bold text-emerald-900">Health Journey</div><div className="text-xs text-amber-700">by FG4U</div></div></Link><nav className="hidden items-center gap-1 md:flex">{nav.map(([href,label,Icon])=><Link key={href} href={href} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-semibold ${path===href?'bg-emerald-50 text-emerald-800':'text-slate-600 hover:bg-slate-50'}`}><Icon size={17}/>{label}</Link>)}</nav><button onClick={()=>setOpen(!open)} className="rounded-xl p-2 md:hidden" aria-label="เมนู">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t bg-white p-3 md:hidden">{nav.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="flex items-center gap-3 rounded-xl p-3 font-semibold text-slate-700"><Icon size={18}/>{label}</Link>)}</nav>}</header><div>{children}</div><footer className="mt-16 border-t border-emerald-100 bg-white"><div className="mx-auto max-w-6xl px-5 py-8 text-sm text-slate-500"><strong className="text-emerald-800">Health Journey by FG4U</strong><br/>Prototype v0.1 • ข้อมูลเพื่อการเรียนรู้และการโค้ช ไม่ใช่การวินิจฉัยหรือการรักษา</div></footer></>}
