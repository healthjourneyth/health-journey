'use client';
import Link from 'next/link';
import Image from 'next/image';
import { usePathname } from 'next/navigation';
import { LayoutDashboard, ClipboardCheck, MessageCircle, Route, Menu, X } from 'lucide-react';
import { useState } from 'react';
const nav=[['/assessment','ประเมินสุขภาพ',ClipboardCheck],['/dashboard','วันนี้ของฉัน',LayoutDashboard],['/journey','เส้นทางของฉัน',Route],['/ai-coach','คุยกับเพื่อน',MessageCircle]] as const;
export function AppShell({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false); const path=usePathname(); return <><header className="sticky top-0 z-50 border-b border-emerald-100/70 bg-[#fffefa]/95 backdrop-blur"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5"><Link href="/" className="flex items-center gap-3"><Image src="/health-journey-logo.svg" alt="Health Journey by FG4U" width={58} height={58} className="h-12 w-12" priority/><div><div className="font-serif text-xl font-bold text-emerald-900">Health Journey</div><div className="text-xs font-medium text-[#b98216]">by FG4U</div></div></Link><nav className="hidden items-center gap-1 md:flex">{nav.map(([href,label,Icon])=><Link key={href} href={href} className={`flex items-center gap-2 rounded-full px-4 py-2.5 text-sm font-bold transition ${path===href?'bg-emerald-50 text-emerald-800':'text-slate-600 hover:bg-emerald-50/60 hover:text-emerald-800'}`}><Icon size={17}/>{label}</Link>)}</nav><button onClick={()=>setOpen(!open)} className="rounded-xl p-2 md:hidden" aria-label="เมนู">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t bg-white p-3 md:hidden">{nav.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="flex items-center gap-3 rounded-2xl p-4 font-bold text-slate-700"><Icon size={18}/>{label}</Link>)}</nav>}</header><div>{children}</div><footer className="mt-16 border-t border-emerald-100 bg-[#fbfaf5]"><div className="mx-auto max-w-7xl px-5 py-8 text-sm text-slate-500"><strong className="text-emerald-800">Health Journey by FG4U</strong><br/>Prototype • ข้อมูลเพื่อการเรียนรู้และการโค้ช ไม่ใช่การวินิจฉัยหรือการรักษา</div></footer></>}
