'use client';
import Link from 'next/link';
import Image from 'next/image';
import { usePathname } from 'next/navigation';
import { LayoutDashboard, ClipboardCheck, MessageCircle, Route, Menu, X, Heart } from 'lucide-react';
import { useState } from 'react';
const nav=[['/assessment','ประเมินสุขภาพ',ClipboardCheck],['/dashboard','วันนี้ของฉัน',LayoutDashboard],['/journey','เส้นทางของฉัน',Route],['/ai-coach','คุยกับเพื่อน',MessageCircle]] as const;
export function AppShell({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false); const path=usePathname(); return <><header className="sticky top-0 z-50 border-b border-[#eee5d4] bg-[#fffdf8]/95 backdrop-blur"><div className="mx-auto flex h-[74px] max-w-[1500px] items-center justify-between px-5 md:px-8"><Link href="/" className="flex items-center gap-3"><Image src="/health-journey-logo.svg" alt="Health Journey by FG4U" width={50} height={50} className="h-11 w-11 object-contain" priority/><div><div className="font-serif text-[19px] font-semibold tracking-[-.02em] text-[#165a3e]">Health Journey</div><div className="text-[11px] font-medium text-[#b98216]">by FG4U</div></div></Link><nav className="hidden items-center gap-1 md:flex">{nav.map(([href,label,Icon])=><Link key={href} href={href} className={`flex items-center gap-2 rounded-full px-4 py-2.5 text-[14px] font-medium transition ${path===href?'bg-[#f1f6e8] text-[#176646]':'text-[#647068] hover:bg-[#f5f4eb] hover:text-[#176646]'}`}><Icon size={16}/>{label}</Link>)}</nav><button onClick={()=>setOpen(!open)} className="rounded-xl p-2 text-[#176646] md:hidden" aria-label="เมนู">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t border-[#eee5d4] bg-[#fffdf8] p-3 md:hidden">{nav.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="flex items-center gap-3 rounded-2xl p-4 font-medium text-[#526158]"><Icon size={18}/>{label}</Link>)}</nav>}</header><div>{children}</div><footer className="mt-16 border-t border-[#eee5d4] bg-[#faf7ee]"><div className="mx-auto max-w-7xl px-5 py-8 text-sm text-[#7a847d]"><strong className="text-[#176646]">Health Journey by FG4U</strong><br/>เรียนรู้สุขภาพ • ลงมือทำทีละนิด • เดินไปด้วยกัน ❤️</div></footer></>}
